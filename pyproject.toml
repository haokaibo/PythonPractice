[project]
name = "PythonPractice"
version = "0.1.0"
description = "This is the project for my Python practice."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
]

[tool.pytest.ini_options]
pythonpath = [
  "."
]
